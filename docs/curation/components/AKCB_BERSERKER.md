# AKCB_BERSERKER — components

Listed at installed standalone version **2.0** (latest standalone: **2.1**).
1 entries, 1 installed. ✓ = installed. Subgroup = choose one.

## Consolidation note

The released Chris Artisan fork `chriz-v1.3.1` already contains the Berserker rebalance,
so the collection must not install this standalone component on top of that fork. Keep
the standalone row unavailable and source the behavior through Artisan component `1003`.
If the collection ever returns to unmodified upstream Artisan, this component would
instead be a strongly recommended conditional child of `1003`.

| # | Component | Group | Subgroup | ✓ | Decision |
|---|---|---|---|---|---|
| 0 | Berserker Overhaul Rebalance: no self-harm, rage immunities, low-HP resistances, Hardiness restored |  |  | ✓ | |
