# AKCB_SHAPESHIFTER — components

Listed at installed version **1.0**.
1 entries, 1 installed. ✓ = installed. Subgroup = choose one.

## Consolidation note

This fix is automatic only when Artisan's custom werewolf sprite path (`5110` or `5111`)
is selected; it is not a global requirement for every Artisan installation. The current
implementation is local and unreleased. Absorb it into the next Chris Artisan-fork
release, then omit the standalone installer. Until that release is fetchable, the
dependent feature remains unavailable. Its future behavior is a hidden conditional
repair, not a globally mandatory component merely because Artisan is selected.

| # | Component | Group | Subgroup | ✓ | Decision |
|---|---|---|---|---|---|
| 0 | Shapeshifter Overhaul Rebalance: greater werewolf uses the humanoid pathfinding footprint (fits through doors) |  |  | ✓ | |
