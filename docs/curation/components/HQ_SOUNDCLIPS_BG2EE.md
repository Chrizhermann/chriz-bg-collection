# HQ_SOUNDCLIPS_BG2EE — components

Listed at installed version **1.3**.
1 entries, 1 installed. ✓ = installed. Subgroup = choose one.

## Integration note

This component is BG2:EE/EET-only. It deliberately replaces same-named EE-NPC and
Black Pits II voice resources, so install it before any later mod that intentionally
overrides those lines. Ordinary player soundsets use different resources.

| # | Component | Group | Subgroup | ✓ | Decision |
|---|---|---|---|---|---|
| 0 | Install high quality soundclips for new BG2EE content |  |  | ✓ | default |
