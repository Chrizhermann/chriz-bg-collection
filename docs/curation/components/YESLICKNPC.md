# YESLICKNPC — components

Listed at installed version **v5.0**.
2 entries, 1 installed. ✓ = installed. Subgroup = choose one.

## UI/dependency notes

- When the parent Yeslick mod is selected, exactly one of components `0` and `1` must be
  installed. Component `1` is the default; disabling the parent mod is the only "none"
  choice.
- `YESLICK_KELDORN_DISPEL_FIX` is a separate single-component collection fix and is not
  catalogued here. Its rebuilt Yeslick dispel uses 1.5x caster-level scaling, matching
  SCS component `3540` rather than alternative `3541`; keep those selections aligned.

| # | Component | Group | Subgroup | ✓ | Decision |
|---|---|---|---|---|---|
| 0 | Yeslick is a regular Fighter-Cleric |  | Yeslick NPC for BGII: SoA & ToB |  | optional |
| 1 | Yeslick is a Fighter-Alaghor of Clangeddin (cleric kit) |  | Yeslick NPC for BGII: SoA & ToB | ✓ | default |
