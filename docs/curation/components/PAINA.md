# PAINA — components

Listed at target version **v1.9** (inferred from the pinned archive; WeiDU emits no
version string).
1 entries, 1 installed. ✓ = installed. Subgroup = choose one.

## UI/dependency notes

- Component `0` is the sole core route and is included automatically when Pai'Na is
  selected. Restrict the parent to BG2:EE/EET.
- If Artisan Hivemaster `5002` is selected, install it before Pai'Na. Pai'Na detects the
  existing kit and skips its bundled copy, so the two components are compatible and do
  not need an exclusivity rule.

| # | Component | Group | Subgroup | ✓ | Decision |
|---|---|---|---|---|---|
| 0 | Pai'Na NPC for BG2:EE |  |  | ✓ | mandatory |
