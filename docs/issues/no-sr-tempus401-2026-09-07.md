# No-SR Tempus 401 failure and supervised recovery

Christopher authorized publishing the BG Rebalance fix, updating the collection,
and finishing the existing no-Spell-Revisions test on 2026-09-07. Do not rebuild
the game or change other component choices to hide this failure.

## Diagnosis

The frozen alpha.12 recipe requested BG Rebalance v0.3.1 components
`101 121 400 401 404 405 407 408`. Component 401 failed; the other seven were
appended after an unchanged 372-component BG2 prefix. BG1 has 27 components.
The original terminal ledger is 214 (`fresh_copy_required`), not a completed run.

The installed Improved Haste wrapper delegates via opcode 146 to `SPWI613A`;
401's direct-only classifier did not find the child's doubling effect. The mod
owner also reproduced a second non-SR Holy Power cleanup-prefix mismatch once
classification passed. Fix commit `e79e775626108ea64510e7d9e3e6b1feedbd49a7`
has 281 passing isolated tests, including captured no-SR resources and base-game
fixtures. This is installer evidence, not live gameplay acceptance.

Original attempt evidence lives under
`.chriz/attempts/attempt-e6b6f3a5aacc11891051/steps/0106-fab86e09514d1f0a/attempt-0001`.
Preserve its before/after logs, output and process evidence. The terminal receipt's
empty successful-attempt list does not mean those raw files are absent.

## Approved bounded recovery

Use a new immutable BG Rebalance v0.3.2 release, verified by archive hash and
packaged source. Preserve a scoped pre-repair backup and hash-bind the original
ledger, recipe, terminal receipt and attempt evidence. Use the existing supervised
recovery API; never rewrite history to mark the original failure successful.

1. Verify the exact current seven-row top tail and original source/tool identities.
2. Uninstall only that top BG Rebalance tail, then verify the 372-row historical
   prefix and affected-file rollback. Do not cascade through earlier mods.
3. Stage the verified replacement and install the original eight selected components.
4. Finish the three original remaining runs: BG Modpack, CDTweaks 2312, BuffBot 1/0.
5. Verify all 429 selected rows (BG1 27 / BG2 402), postconditions and process evidence;
   publish a separate recovery completion receipt and retain the game for testing.

The original failed 401 rollback was WeiDU-reported, not independently byte-proven.
Keep that distinction even if the later supervised rollback and completion pass.
Automatic public Resume is not introduced by this operator-assisted procedure.

## Release scope

App alpha.15 / collection alpha.13 are intended to carry only the new Rebalance pin,
with the 434-component recommended default unchanged. No Bardic Wonders update,
new components, global cleanup or unrelated mod changes are part of this patch.
Publication and actual recovery results must be recorded before calling them complete.

## Completed recovery — 2026-09-07, 19:34 KST

BG Rebalance v0.3.2 was released at commit
`632af10873d54af649e9982724b5c351bd34075e`. The independently downloaded ZIP is
1,369,825 bytes, SHA256
`25480a8e597d316d3cf1799f641971f3b6edb113eea24da7f45a8dd70b0a9ef4`.

The exact existing `ChrizEasyBG-No-SR-Test` copy was recovered with
`tools/recover-no-sr-tempus.ps1`. A 124-file, 12,604,454-byte scoped ZIP backup and
the original protected history are retained under
`.chriz/recoveries/tempus-v032-20260907/`. The seven-component rollback restored
the 372-row prefix and passed the affected-file byte checks. The replacement's
eight components and all three remaining runs then installed with process exit 0.
No full restart, earlier mod reinstall, or source-game modification occurred.

The existing Rust recovery validator independently accepted the five operation
records and all 429 exact planned rows. The acceptance example then finalized
isolated save identity and published a separate `supervised_recovery` receipt.
Its effective version is correctly `0.1.0-alpha.12 (repaired)`, with the v0.3.2
artifact replacement recorded, not a false claim to be an original alpha.13 run.
The original terminal ledger/receipt remains `fresh_copy_required` and unchanged.

Launch `game/InfinityLoader.exe`. The completed installation is retained for
Christopher; live gameplay, save/reload and the no-SR Tempus behavior have not
been tested by this recovery procedure. Do not run the recovery stages again.
