{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal core access for the installer shell's main window.",
  "windows": ["main"],
  "permissions": ["core:app:default"]
}
